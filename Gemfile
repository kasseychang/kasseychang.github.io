source "https://rubygems.org"

# Jekyll + the plugin set GitHub Pages uses, pinned to match production.
gem "github-pages", group: :jekyll_plugins

# Multilingual support (not on the GitHub Pages allowlist — requires the
# GitHub Actions build, which runs `bundle exec jekyll build`).
gem "jekyll-polyglot", group: :jekyll_plugins

gem "webrick"
